# Autonomous Workflow Rules

- Apply code modifications, run builds (`pio run`), and execute verification tools autonomously without asking for confirmation on routine steps.
- Do not commit changes automatically to git; suggest commits when enough changes accumulate.
- Compile firmware after changes to ensure `merged-firmware.bin` is updated.
- Communicate concisely in Slovak.
